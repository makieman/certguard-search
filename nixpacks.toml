[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["cd certguard && pip install -r requirements.txt"]

[start]
cmd = "cd certguard && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
